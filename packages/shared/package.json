{
  "name": "@dydyd/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for DYDYD",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
